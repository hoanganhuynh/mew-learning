import { DialogueTopic } from '@/types';

/**
 * Built-in starter topics.
 * Currently empty — import your own topics via the Import / Generate panel.
 */
export const mockDialogues: DialogueTopic[] = [];
